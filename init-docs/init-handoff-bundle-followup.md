{{BUNDLE_STEP}}. Because the rqmd Copilot bundle is currently `{{BUNDLE_STATE}}`, also run `{{BUNDLE_COMMAND}}`.
{{BUNDLE_ARTIFACT_STEP}}. If that preview is truncated too, rerun it as `{{BUNDLE_ARTIFACT_COMMAND}}` and read that file directly.
{{BUNDLE_SKILLS_STEP}}. Use that bundle interview to generate or refine the project-local `/dev` and `/test` skills before finishing setup.
{{BUNDLE_APPLY_STEP}}. Apply the bundle install only after the user confirms the preview.
